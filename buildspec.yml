version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd Questera-Backend && npm install
  build:
    commands:
      - echo "Build complete"
artifacts:
  files:
    - 'Procfile'
    - 'Questera-Backend/**/*'
  discard-paths: no

